apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: backend-service
spec:
  selector:
    app: expense-tracker-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
